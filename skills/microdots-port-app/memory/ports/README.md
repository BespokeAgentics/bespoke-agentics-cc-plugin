# Port records

One file per completed (or honestly-stopped) port run, named
`<date>-<slug>.md`, written by Phase 8 per `references/memory-protocol.md`.
These records are what makes the next similar port more deterministic — do not
edit them after the fact; corrections are dated annotations.
