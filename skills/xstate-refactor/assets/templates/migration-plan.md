# XState Migration Plan — {TARGET_SLUG}

> Generated by `bespoke-agentics:xstate-refactor` on {DATE}. Executable by a fresh session with no
> other context. Source analysis: `{ANALYSIS_DIR}/behavior-map.md`, `{ANALYSIS_DIR}/state-model.md`.

## Summary

- **Target**: {files}
- **Style**: {flat machine | statechart | actor system} — {one-line why}
- **States**: {N} · **Events**: {N} · **Impossible states eliminated**: {N}
- **XState status**: {v5 installed | to install | v4 present → decision below}
- **Interview**: {N} confirmations, {N} corrections, {N} UI-gap decisions ({date})

## Environment & setup

| Item | Value |
|------|-------|
| Package manager | {pm} |
| Framework / binding | {React + @xstate/react, ...} |
| Test runner | {vitest, ...} |
| TypeScript | {yes/no} |
| Setup steps | {`<pm> add xstate @xstate/react` into {package}, or "none — installed"} |

{If v4 present: the chosen path (upgrade / alias / v4) and its steps.}

## The statechart

```mermaid
{stateDiagram-v2 from state-model.md, post-interview}
```

## Machine specification

New file: `{path}/{feature}.machine.ts`

{States / Events / Context / Guards & actions / Actors tables from state-model.md, post-interview.}

## Behavioral divergences (user-confirmed)

| # | Old behavior | New behavior | Confirmed | Test |
|---|--------------|--------------|-----------|------|

## UI coverage matrix (final)

{Matrix per references/ui-coverage.md — no ⚠ rows except user-deferred ones, which also appear
under Open Questions.}

## Implementation steps

Ordered; the app stays runnable after each step.

1. **Setup** — {or "skip"}
2. **Machine in isolation** — write `{feature}.machine.ts`; no UI changes.
3. **Tests (machine)** — `{feature}.machine.test.ts`: path coverage via `createTestModel`
   (`xstate/graph`), unit tests per guard boundary + divergence. Green before proceeding.
4. **UI wiring** — per-touchpoint list with `file:line`: {old read/handler} → {useMachine/
   useSelector/send/state.matches/state.can}. UI coverage tests added here.
5. **Deprecation** — checklist below.
6. **Verification** — commands below.

## Deprecation checklist

| Item (file:line) | Kind | Replaced by | Action | Done |
|------------------|------|-------------|--------|------|

Mode: {delete outright | deprecate-then-delete (consumers: ...) | flagged (criterion: ...)}

## Test plan

| Layer | File | Covers |
|-------|------|--------|
| Path coverage | {feature}.machine.test.ts | every state & transition |
| Unit | 〃 | guards at boundaries, context, divergences |
| UI state coverage | {feature}.test.tsx | every matrix row |

Baseline: existing tests {list} recorded green on {date}; must stay green except {divergence tests}.

## Verification commands

```sh
{typecheck}
{lint}
{test}
```

## Feasibility register

| Area | Status | Notes |
|------|--------|-------|
{🟢 OOTB · 🔵 Config · 🟡 Custom Dev · 🔴 Gap · ⚪ TBD}

## Open questions

## Risks
