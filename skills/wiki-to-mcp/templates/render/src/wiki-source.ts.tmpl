// Deprecated placeholder. The WikiSource interface and GitWikiSource impl now
// both live in src/wiki.ts. This file exists only because the template-staging
// area can't have files removed; it should NOT be copied into a generated
// project (see SKILL.md Phase 5 "files starting with wiki-source").
export {};
