// This file is overwritten by the deploy-target template's own src/wiki.ts.
// It exists in _common/ only so the skill's copy step always has a fallback;
// in any generated project, src/wiki.ts comes from render/, workers/, or docker/.
//
// If you're seeing this comment in a generated project, the target-specific
// wiki.ts was not copied. Re-run the wiki-to-mcp skill.
export {};
