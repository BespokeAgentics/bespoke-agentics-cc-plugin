<!--
Template: AGENTS.md pointer. Written into every directory that gets a CLAUDE.md.
AGENTS.md is the cross-tool convention; CLAUDE.md is the source of truth here, so this
file just redirects. Do NOT duplicate the CLAUDE.md body — pointing prevents drift.
The whole file is managed; it can be regenerated wholesale on refresh.
-->

<!-- progressive-disclosure:managed -->
# Agent instructions

The agent and AI-assistant instructions for this directory live in
[`./CLAUDE.md`](./CLAUDE.md), which is the single source of truth.

If your tool reads `AGENTS.md` but not `CLAUDE.md`, open `./CLAUDE.md` in this same
directory for the project context, stack, commands, and conventions that apply here.

> Maintained by the `progressive-disclosure` skill. Edit `CLAUDE.md`, not this file.
<!-- /progressive-disclosure:managed -->
